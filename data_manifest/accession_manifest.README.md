# Accession Manifest Note

The full accession manifest is part of the Zenodo `Sequences` bundle.

GitHub carries split, label, feature, checksum, and freeze manifests. Protein
and genome accessions, source database names, freeze date, retrieval scripts,
and checksums are packaged with the Zenodo data release.
